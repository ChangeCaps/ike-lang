fn assert : bool -> str -> {}
fn assert condition message {
  match condition {
    true  -> {}
    false -> panic message
  }
}

fn panic : 'a -> 'a
fn panic message {
  io::print "thread main panic: `"
  io::print <| debug::format message
  io::println "`"
  os::exit 1
}

