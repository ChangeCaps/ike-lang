fn main {
  match [] {
    bar::b _ -> {}
  }

  let f = foo 1 2, 2
  let y = "{f} {f}"

  let f = foo {
    a: 100
    b: "hello world!"
  }

  let f = f with {
    a: (200)
  }

  let a = [0; 1; 2; ..[0]]
}

fn foo x y -> x + y

type foo = { a: int; b: str }

type bar = bar::a int | bar::b
